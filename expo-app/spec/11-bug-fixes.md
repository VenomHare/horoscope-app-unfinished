# Bugs
- When time changes the hora doesn't recalculates which is expected but the hora doesn't changes as well, when time changes the hora should change on the basis of cached table. 
- When we already have hora table and user selects alert for start of guru before 10 mins and guru is at 10:10 and 14:10 then the app should  schedule notification at 10:00 and 14:00 and vice versa on hora ending, on every recalculate the notifications should get cleared and re-schedule all notifications.
- These notification should have higher priority (i.e. i want the mobile to play notification sound and it pops up on top).
- Sticky Notification should be turned on by default
- Widget is too large it should be smaller in size, the hora icon should be in background of whole  widget at the right side, The Hora name should be bigger and the background color of widget should be light in color and icon should be dark.
